<article class="tm-feature-card drift-card" style="animation-delay: {{ delay }};">
    <p class="tm-card-eyebrow">{{ eyebrow }}</p>
    <p class="tm-card-title">{{ title }}</p>
    <p class="tm-card-copy">{{ description }}</p>
</article>