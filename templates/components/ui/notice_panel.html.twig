<div class="tm-notice-panel page-fade {{ className }}" style="animation-delay: {{ delay }};">
    <p class="tm-section-eyebrow">{{ eyebrow }}</p>
    <h3 class="tm-notice-title">{{ title }}</h3>
    <p class="tm-notice-copy">{{ description }}</p>
</div>