<div class="page-fade" style="animation-delay: {{ delay }};">
    <p class="tm-section-eyebrow">{{ eyebrow }}</p>
    <h2 class="tm-section-title">{{ title }}</h2>
    <p class="tm-section-copy">
        {{ description }}
    </p>
</div>